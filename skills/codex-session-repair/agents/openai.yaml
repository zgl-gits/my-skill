interface:
  display_name: "Codex Session Repair"
  short_description: "Repair bloated Codex thread history safely"
  default_prompt: "Use $codex-session-repair to diagnose and safely repair a Codex thread that cannot compact or resume."
policy:
  allow_implicit_invocation: true
